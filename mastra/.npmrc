# Suppress ERESOLVE peer-dependency warnings from @mastra/core's pinned
# @ai-sdk/ui-utils-v5 (still declares zod ^3.23.8 while the root uses zod v4).
# npm auto-overrides the conflict; these are non-blocking peer warnings only.
loglevel=error
