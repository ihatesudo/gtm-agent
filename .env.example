# Recommended model provider: OpenRouter (OpenAI-compatible; choose a lower-cost
# model in OPENROUTER_MODEL instead of using Gemini directly).
PROVIDER=openrouter
GENAI_PROVIDER=openrouter
OPENROUTER_API_KEY=sk-or-v1-replace-me
OPENROUTER_MODEL=openrouter/auto
# Optional attribution after deployment; leave blank during local development.
OPENROUTER_SITE_URL=

# Alternative Gemini / Vertex AI configuration
# Google AI Studio / Gemini API key (prepaid)
# GEMINI_API_KEY=
# GOOGLE_API_KEY=
# GOOGLE_GENERATIVE_AI_API_KEY=

# To use your $300 GCP Trial Credits via Vertex AI instead of AI Studio:
# GENAI_PROVIDER=vertex or api
# GOOGLE_GENAI_USE_VERTEXAI=true or false
# GOOGLE_CLOUD_PROJECT=
# GOOGLE_CLOUD_LOCATION=
#
# Auth (pick one):
#   A) Service account JSON (project convention). Newlines inside the key are OK.
# GOOGLE_APPLICATION_CREDENTIALS='{ "client_email": ..., "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n", "private_key_id": ... }'
#
#   B) Same service account as separate fields (the @ai-sdk/google-vertex/edge
#      defaults). GOOGLE_PRIVATE_KEY may use literal \n; it is normalized at
#      runtime. Either A or B works in Node AND Cloudflare Workers.
# GOOGLE_CLIENT_EMAIL=
# GOOGLE_PRIVATE_KEY=
# GOOGLE_PRIVATE_KEY_ID=
#
#   C) Express mode — a single API key, no service account, no JWT signing.
# GOOGLE_VERTEX_API_KEY=

# Code plan (mastra studio)
# ZHIPU_API_KEY=

# Appwrite Cloud Magic URL auth. Both values are safe to expose to the browser;
# never add an Appwrite API key to this file or to Reflex Cloud's public config.
APPWRITE_ENDPOINT=https://<region>.cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=<project-id>

# Hosting URLs. These are not secrets. Set them while running `make hosting-build`;
# set REFLEX_CORS_ALLOWED_ORIGINS on Render to the final Appwrite Sites URL.
# BACKEND_URL=https://your-api.onrender.com
# SITE_URL=https://your-site.appwrite.network

# Turso DB Configuration (used by Mastra storage)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=